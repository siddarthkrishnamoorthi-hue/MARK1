Report Analysis + ICT Trade Frequency Breakdown

What your backtest tells me is critically wrong:
Your EA ran for 27.5 months (595 trading days, ~119 weeks) and produced only 11 completed trades. That is:

Per day: 0.018 trades — effectively dormant
Per week: 0.09 trades — trades roughly once every 11 weeks
Per month: 0.4 trades — less than 1 trade per month

ICT concepts should realistically produce 2 to 3 setups per day, Inner Circle Trader1 per session, and up to 15 per week. Your EA is firing at roughly 2–3% of its theoretical capacity.

What ICT properly executed should produce on EURUSD:
Statistically there are 10 sessions per week — each offering a potential setup across London and NY killzones, though levels are not hit every day and patience is required. MQL5

Per day: 1–2 quality, multi-confluence trades (one best per session — London or NY, not both unless both confirm)
Per week: 3–8 executed trades (with strict discipline filtering ~60% of raw signals)
Per month: 12–25 executed trades (on EURUSD alone, single pair, strict ICT rules)

Tick-data studies show up to 80% of EURUSD liquidity sweeps during the London killzone reverse within 15 minutes, offering setups that rarely exceed a 10-pip drawdown when executed with strict order-block rules.

With the full Silver Bullet model added back, that adds 3 more windows per day. So realistically targeting 1 trade per session window (London SB + NY AM SB + NY PM SB) = 3 possible, take the 1 best per day = 20–22 trades per month.

You are a autonomous senior quantitative developer and ICT (Inner Circle Trader) methodology expert. You will rebuild and complete a professional-grade, fully automated Expert Advisor (EA) for EURUSD on MetaTrader 5 using MQL5 as the primary language, with optional Python bridge for data logging and analytics. The goal is a production-ready EA that correctly implements every major ICT institutional concept and achieves realistic ICT-consistent trade frequency: 1 to 2 high-quality trades per day, 3 to 8 per week, and 12 to 25 per month on EURUSD — not the anemic 0.4 trades per month the current broken version is producing.
Your task is NOT to rewrite the EA from scratch. Your task is to AUDIT the existing Expert Advisor codebase, identify every gap between what is coded and what the ICT institutional trading concept requires, then implement every missing or broken piece directly into the existing code or as new companion files that integrate with it. Use whatever language best fits each component — MQL5 for MT5 execution logic, Python for analytics and signal pre-processing, and JSON or CSV for configuration — but everything must connect to and enhance the existing EA without breaking what already works.

STEP 1 — READ AND AUDIT FIRST, CODE NOTHING YET
Before writing a single line of code, read every file in this directory recursively. Understand the complete existing architecture: what inputs exist, what session logic is present, what detection functions are implemented, what order management exists, and what is merely stubbed or disabled. Then produce a structured audit report in this exact format as a markdown file named ICT_AUDIT_REPORT.md:
Section A — What Is Correctly Implemented: list every ICT concept that is coded and working correctly with the function name and file where it lives.
Section B — What Is Coded But Broken Or Misconfigured: list every ICT concept that exists in code but is producing wrong results, with the specific bug description. Based on the backtest report I provide, known broken items include: FVG entry placement at 25% of gap instead of 50% CE level, fixed pip stop loss of 20 pips instead of structural sweep-wick placement, RR delivery failing (0.74:1 achieved vs 1.5 target set), and Silver Bullet module disabled via input parameter.
Section C — What Is Missing Entirely: list every ICT concept that does not exist anywhere in the codebase and needs to be built.
Section D — Trade Frequency Root Cause Analysis: explain specifically why the EA produced only 11 trades in 27 months (595 trading days) instead of the expected 300+ trades. Identify every filter, condition, or disabled module that suppressed signal generation.
Section E — Implementation Plan: list every change you will make, ordered by impact on trade frequency and profitability, with the file you will modify or create for each change.
Do not proceed past Step 1 until the audit report is complete and written to disk.

STEP 2 — FIX ALL BROKEN EXISTING LOGIC IN ORDER OF PRIORITY
Priority 1 — Fix Stop Loss Placement. Find every place in the code where stop loss is calculated as a fixed pip value (any variable that multiplies a pip value by a constant like 20.0 or uses InpSLPips directly as the SL distance). Replace every instance with structural SL placement: the SL must be set to the price of the candle wick that swept the liquidity pool plus a 2 pip buffer, retrieved from the sweep detection logic. If no sweep wick price is stored at the time of entry, use the most recent swing extreme on M15 beyond the entry zone. Add a hard maximum cap of 28 pips as a safety limit only — never as the default. Write a function ComputeStructuralSL(direction, sweepWickPrice, bufferPips) that encapsulates this and replace all fixed SL calculations with calls to it.
Priority 2 — Fix FVG Entry Level. Find every line where the FVG entry price is computed. Replace 25% entry with 50% Consequent Encroachment level. The CE is (FVG_top + FVG_bottom) / 2. Also add a secondary limit order at 62% of the FVG as a fallback if the 50% order is not filled within 3 M15 candles. Add a tertiary at 79% only if an OB or Breaker Block also exists at that level. Reduce order expiry from the current value (10 bars) to 3 M15 bars for Silver Bullet setups and 5 M15 bars for standard FVG setups — stale setups must not sit open for 10 bars because market context changes completely in that time.
Priority 3 — Enable and Fix Silver Bullet. Find the Silver Bullet input parameter (InpEnableSilverBullet) and set its default to true. Then audit the Silver Bullet logic itself. It must follow this exact sequence and nothing else: during the defined Silver Bullet time window only, detect a sweep of equal highs or equal lows formed within the prior 60 to 90 minutes, then detect a displacement candle (body at least 70% of total range, minimum 8 pips body) in the direction of the daily bias following the sweep, then detect an FVG formed by that displacement on M5, then place a limit buy or sell at the CE (50%) of that FVG with SL at the sweep wick plus 2 pips and TP1 at 20 pips minimum. If the Silver Bullet FVG entry is not hit within 4 M5 candles (20 minutes), cancel the order — the window has passed. The Silver Bullet must fire during three daily windows: 08:00 to 09:00 GMT (London), 15:00 to 16:00 GMT (NY AM), and 19:00 to 20:00 GMT (NY PM). Each window is independent. Implement them as separate state machine states so a failed London Silver Bullet does not block the NY Silver Bullet.
Priority 4 — Fix RR Delivery. Find the take profit calculation. The current EA is not reaching its stated 1.5 RR target. Audit whether TP is being placed correctly as entry price plus (SL distance × RR ratio) for sells or minus for buys. If the TP math is correct but not being hit, the problem is that the SL is too tight (getting hit first) — this will be resolved by Priority 1. Additionally implement a three-tier TP system to replace any single-TP logic: TP1 closes 35% of position at 1:1 RR and moves SL to break-even plus 1 pip, TP2 closes 40% at 1:2.5 RR, TP3 closes the final 25% at the next liquidity pool target (PDH/PDL, PWH/PWL, or equal highs/lows beyond). Never close a full position at a single TP level — partial scaling is mandatory for ICT-consistent trade management.
Priority 5 — Enable All Disabled Filters With Correct Logic. Find every input with a default of false that corresponds to an ICT filter: H4 filter, PD (Premium/Discount) filter, Weekly Bias filter, OB filter, Trend filter. For each one: first read the existing implementation of that filter. If the implementation is logically correct, change the default to true and tune the parameters. If the implementation is wrong or incomplete, fix it before enabling it. The H4 filter must check that the last 3 completed H4 candle closes form a higher high or lower low structure consistent with the daily bias — not just a moving average crossover or any simplification. The PD filter must compute the midpoint of the H4 session range and only allow longs when current price is below the midpoint (discount) and shorts when above (premium). The OB filter must require that the entry zone is within or adjacent to a valid unviolated order block on M15 or H1. Enable all of these together — they work as confluence gates, not as individual standalone filters, and enabling them together will increase quality without reducing frequency because they filter out bad setups while the Silver Bullet fix simultaneously adds new valid ones.

STEP 3 — BUILD ALL MISSING ICT MODULES
After all broken logic is fixed, build each missing module as a new file. For MQL5 include files (.mqh), place them in the same directory as the main EA and add the appropriate include directive to the main .mq5 file. For Python utility scripts, place them in a /tools subdirectory.
Missing Module A — Judas Swing Detection (if not already present and working):
Create a function or class that at London open (07:00 GMT) marks the Asian session high and low (from the prior 21:00 to 00:00 GMT window), then monitors every M5 candle close for a penetration of Asian Low by 3+ pips followed by a close back above Asian Low within 3 candles (bullish Judas Swing confirmation) or a penetration of Asian High by 3+ pips followed by a close back below (bearish Judas Swing confirmation). When confirmed, set a flag JudasSwingActive=true and store the sweep wick price and the confirmed direction. This flag enables entry logic for the rest of the London session. Reset at NY open. If no Judas Swing by 08:30 GMT, check for a sweep of the prior day's London High or Low instead as a fallback.
Missing Module B — Power of Three Session State Machine (if not present):
Implement an enum SessionPhase with values: PHASE_ASIAN, PHASE_MANIPULATION, PHASE_DISTRIBUTION, PHASE_INACTIVE. Transition rules: enter PHASE_ASIAN at 21:00 GMT, enter PHASE_MANIPULATION at 07:00 GMT (London open), transition to PHASE_DISTRIBUTION when Judas Swing is confirmed OR when London session produces a clear BOS on M15 in the bias direction, enter PHASE_INACTIVE after NY close at 20:00 GMT. Entry logic is only triggered during PHASE_MANIPULATION (after Judas Swing) or PHASE_DISTRIBUTION. No entries during PHASE_ASIAN or PHASE_INACTIVE.
Missing Module C — PDA Confluence Scoring Engine (if not present):
Create a function int ComputePDAScore(double priceZone_high, double priceZone_low, ENUM_ORDER_TYPE direction) that evaluates the following and returns a total integer score: if a valid unviolated M15 OB overlaps with the zone add 2, if a valid H1 OB overlaps add 2, if a Breaker Block overlaps add 3, if an unmitigated M15 FVG overlaps add 1, if an IFVG overlaps add 2, if a BPR overlaps add 3, if the zone is in Discount (for longs) or Premium (for shorts) add 1, if the zone aligns with a liquidity sweep wick within 3 pips add 2, if current time is within a Killzone window add 2, if current time is within a Macro window add 1. Minimum score to place a trade: 5. Score 7 or above: full risk. Score 5 to 6: half risk. Score below 5: no trade.
Missing Module D — Liquidity Pool Map (if not present or incomplete):
Build a structure or array that maintains all active liquidity pools. Each pool has: price level, type (BSL above equal highs or SSL below equal lows), number of touches or equal levels, session of origin, swept status boolean, and sweep timestamp. Populate this map at each session start by scanning the prior 48 H1 bars for: equal highs within 3 pip tolerance (minimum 2 touches = BSL), equal lows within 3 pip tolerance (minimum 2 touches = SSL), previous day high (PDH), previous day low (PDL), previous week high (PWH), previous week low (PWL), current Asian High and Low. Update the swept status whenever a sweep is detected. Use this map as the primary source for SL placement, TP targets, and Judas Swing reference levels.
Missing Module E — Python Performance Analytics (new file /tools/ICT_Analytics.py):
Read the EA's CSV trade log (which you will ensure the EA writes to disk — add file logging if not present). Produce the following analysis on demand: trade frequency by session (London SB, NY SB, Judas Swing, OB, FVG), daily and monthly trade counts versus the 1 to 2 per day target, win rate by setup type, average RR by setup type, PDA score distribution of winning versus losing trades (this validates the minimum score threshold), equity curve with drawdown shading, session performance heat map (day of week × session window as a 5×3 grid colored by average R), and a monthly summary table. Output all analysis to /tools/reports/ as both PNG charts and a CSV summary. This script is the feedback loop that drives EA parameter optimization based on live or backtest results.

STEP 4 — INTEGRATION AND PARAMETER TUNING
After all modules are built and integrated, update the EA's input parameter defaults to these production-optimized values based on the ICT analysis: InpRiskPercent from 0.25 to 0.5, InpRRRatio remove this parameter entirely and replace it with InpTP1R=1.0, InpTP2R=2.5, InpTP3UseLiquidityTarget=true, InpSLPips remove this and use structural SL only with InpMaxSLPips=28 as the cap, InpFVGEntryPercent from 25 to 50, InpEnableSilverBullet from false to true, InpEnableH4Filter from false to true, InpEnablePDFilter from false to true, InpEnableWeeklyBias from false to true, InpEnableOBFilter from false to true, InpEnableTrendFilter from false to true, InpOrderExpiryBars from 10 to 5 for standard setups and 4 for Silver Bullet, InpMinSweepPips from 3.0 — keep at 3.0 this is correct, InpMaxFVGAgeBars from 5 to 8, InpMinFVGPips from 5 — keep, InpMinDisplacementBodyPct from 70 — keep this is correct, InpMinDisplacementPips from 8 — keep, InpMaxOpenTrades from 1 to 2, InpDailyLossLimitPct from 1.5 to 2.0, InpMaxConsecLosses from 3 — keep, InpMaxSpreadPips from 2.5 to 2.0, InpNewsHaltBefore from 30 — keep, InpNewsResumeAfter from 15 — keep.
Add these new parameters that did not exist before: InpMinPDAScore default 5, InpEnableJudasSwing default true, InpEnableOTE default true, InpEnableBPR default true, InpEnableIFVG default true, InpEnableMacroWindows default true, InpSilverBulletMaxSLPips default 15, InpTP1ClosePct default 35, InpTP2ClosePct default 40, InpTP3ClosePct default 25, InpEnableTrailing default true, InpTradeLogPath default empty string meaning use default EA directory.

STEP 5 — VERIFICATION CHECKLIST
After all code changes are complete, run through this checklist and confirm each item in a file named ICT_IMPLEMENTATION_CHECKLIST.md:
Confirm that SL is never a fixed pip distance — always computed from sweep wick.
Confirm that FVG entry is placed at 50% CE with fallback at 62%.
Confirm that Silver Bullet fires in all three windows (London 08:00, NY 15:00, PM 19:00 GMT) independently.
Confirm that Judas Swing detection runs at London open and NY open.
Confirm that PDA scoring is computed before every limit order placement.
Confirm that minimum PDA score of 5 gates every entry.
Confirm that partial TP is applied at TP1 (35%), TP2 (40%), TP3 (25%).
Confirm that SL moves to break-even after TP1 is hit.
Confirm that trailing stop activates after TP1 using M15 structure.
Confirm that daily loss limit of 2% halts trading for the remainder of the day.
Confirm that maximum 2 simultaneous trades is enforced.
Confirm that no trades are placed outside of defined killzone or macro windows.
Confirm that H4 bias, PD filter, weekly bias, OB filter, and trend filter are all enabled by default.
Confirm that the news filter halts entries 30 minutes before high-impact events.
Confirm that the EA writes a CSV log with every trade including: setup type, entry time, session, PDA score, SL distance, TP1/2/3 levels, exit price, RR achieved, and exit reason.
Confirm that ICT_Analytics.py reads the CSV log and produces session frequency analysis.
Confirm that the EA compiles with zero errors and zero warnings in MetaEditor.

STEP 6 — EXPECTED OUTPUT VALIDATION
After the rebuild, the EA run over the same 2024.01.01 to 2026.04.18 period should produce approximately: 250 to 400 total trades (versus 11 in the broken version), profit factor of 1.75 to 2.2, Sharpe ratio of 1.0 to 1.4, maximum drawdown below 12%, win rate between 50% and 65%, average RR of 1.8 to 2.2, and monthly trade frequency of 15 to 30 trades. If a backtest after these changes does not approach these targets, identify which specific module or parameter is the bottleneck — use the PDA score distribution from ICT_Analytics.py to determine if the minimum score threshold needs lowering (too few setups found) or raising (too many losers slipping through) — and adjust accordingly. The target is a live-tradeable, rules-based institutional ICT EA that a funded trader could confidently run on a prop firm account.
Begin now with Step 1. Read the full codebase, then write ICT_AUDIT_REPORT.md before touching any code. then Iterate thr full steps.